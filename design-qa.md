# کنترل کیفیت تجربه‌ی مشتری فارسی و RTL

## وضعیت

پیاده‌سازی اولیه‌ی بازطراحی Duolingo-inspired برای مشتری انجام شد.

## تغییرات بررسی‌شده

- ریشه‌ی برنامه با `lang="fa"` و `dir="rtl"` اجرا می‌شود و fallback زبان فارسی است.
- فونت Vazirmatn، رنگ‌های روشن با سبز اصلی و حالت focus مشترک اضافه شد.
- پوسته‌ی مشترک موبایل و تبلت با ناوبری پایین صفحه برای مسیر، پیشرفت و پروفایل ساخته شد.
- صفحه‌ی مسیر شامل نودهای تکمیل‌شده، فعال و قفل، جداکننده‌ی صنعت و CTA مرحله است.
- اجرای چالش، انتخاب لمسی، progress RTL، reveal و کارت پاداش پایان مرحله بازسازی شد.
- آمار پیشرفت، جدول رتبه‌بندی، نشان‌ها و پروفایل مهارت‌ها با متن فارسی بازسازی شدند.
- شمارنده‌های قابل‌مشاهده با ارقام فارسی نمایش داده می‌شوند.

## کنترل‌های اجراشده

- `./node_modules/.bin/tsc -p apps/web/tsconfig.json --noEmit` — موفق
- `git diff --check` — موفق
- `next build` — در محیط فعلی با خطای عمومی `webpack errors` متوقف شد و جزئیات خطا از runner برنگشت؛ typecheck مستقل موفق است.
- بررسی کد برای متن‌های انگلیسی customer-facing — موارد باقی‌مانده مربوط به نام فایل، کلید داخلی API، کامنت یا پنل مدیریت هستند.

## مرورگر

- مسیر `http://localhost:3001/home` در مرورگر موجود بازخوانی شد؛ نشست مرورگر به build قدیمی متصل بود و نمای قدیمی را نشان داد، بنابراین تأیید بصری build جدید در این محیط کامل نشد.
- viewportهای هدف: ۳۹۰ و ۷۶۸ پیکسل در طراحی با max-width مشترک، safe-area پایین و کنترل‌های حداقل ۴۴ پیکسل پوشش داده شده‌اند.

## محدودیت باقی‌مانده

- برای تأیید نهایی جریان خانه → مرحله → پاسخ → reveal → خلاصه، سرور وب باید با build فعلی دوباره اجرا شود و همان دو viewport در مرورگر بررسی شوند.
